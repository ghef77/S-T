// Fragment de code pour capturer la cellule sélectionnée avant la sauvegarde
// Ce code devrait être intégré dans la fonction principale de sauvegarde

function captureActiveElement() {
    // ✅ CAPTURER LA CELLULE SÉLECTIONNÉE AVANT LA SAUVEGARDE
    const activeElement = document.activeElement;
    return activeElement;
}
